class Box
    int value

    fun void init(int value)
        this.value = value

    fun int get()
        return this.value

    fun int add(Box other)
        return this.value + other.get()

Lock lock
lock.lock()
lock.lock()