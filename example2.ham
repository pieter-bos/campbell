fun (int -> int -> int) dynamicOperation(int op)
    fun int add(int x, int y)
        return x + y

    fun int mul(int x, int y)
        return x * y

    if op == 0
        return add
    else
        return mul

dynamicOperation(1)(5, 6)